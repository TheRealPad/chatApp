spring:
  mvc:
    servlet:
      path: /api
  datasource:
    url: jdbc:mysql://localhost:3306/documentManagement
    username: root
    password: YOURNEWPASSWORD
    driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    org.springframework.security: DEBUG
